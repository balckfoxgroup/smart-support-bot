You build a product catalog JSON for Telegram Assistant Bot.

Rules:
- Output ONLY valid JSON (no markdown fences, no commentary).
- schema_version must be 1.
- product_id: lowercase kebab-case from folder name.
- title/short_summary/long_summary/features must include fa, en, ru, zh keys.
- For Persian (fa): every sentence must start with a Persian word; keep official product names in English Title Case.
- features: 3–8 items with id, title{}, summary{} (summaries short and educational).
- media: list screenshot/hero slots with path "" if unknown; set local_folder to media/catalogs/<product_id>.
- enabled: true
- menu_order: number
- menu_emoji: one emoji
- Include does_not, keywords, support placeholders if unknown.

Return a single JSON object matching the bot product catalog schema.
