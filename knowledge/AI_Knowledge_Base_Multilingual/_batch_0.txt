Q041|Separate AI credit top-up claim codes (`BFXQ-CLM-…`), not a full license mode change. Catalog `ai_quota` is separate_from_license. PAS generates BFXQ claim recharge. Claiming applies quota without changing local license 
Q042|Duration options typically 12–30 months per host catalog; unlock default months often 18 when unspecified. `license-access.json` months: 12,15,18,21,24,27,30. Auth NormalizeLicenseMonths applies catalog. Expired shows “L
Q043|Catalog supports up to 3 devices with discounts; binding rules still apply per claim. multi_device max_devices 3; discounts device2 20%, device3 30%. Each claim binds Device ID on first unlock — do not reuse one claim ac
Q044|Locally and on host, TX already used is rejected. `ErrTxAlreadyUsed` (Persian product string) and host `tx_already_used` / `tx_already_claimed` when claim already bound. After claim, re-entering TX no longer reveals code
Q045|That claim/activation code is already tied to another Device ID. API reason `bound_other_device` / `claim_bound` / `already_bound`. You cannot unlock a second unrelated PC with the same claim.
Q046|Message: “Invalid activation code” / enter code first. Typo, wrong tier prefix, truncated paste, or non-claim garbage. Also wrong device for machine-bound codes.
Q047|Wait for BEP-20 confirmation; amount and recipient must match. Auth errors include TX not found, failed, wrong recipient, insufficient amount, wrong token (not USDT BEP-20), pending confirmation.
Q048|Feature gated until license activation (`dialog.need_activate`). Paid/locked ops show Limited Access / need activate. Free Basic keys still exist, but many buttons require Basic Full or higher.
Q049|Pro-only feature while license is Basic or locked (`dialog.need_pro`). Tunnel chain, some CDN/subscription/node paths require Pro or AI Pro unlock.
Q050|Full license features lock; renew to continue. Badge: “License expired — activate again to renew”. `isLicenseExpired` blocks full license features. Reactivation may return expired.
Q051|Host file `license-access.json` (and hub live pricing). Host-editable catalog — Windows, PAS, and website read it; no app rebuild required for price edits. App falls back to constants if hub pricing missing.
Q052|Enter central SSH credentials and save locally before deployment. Saves host, auth, country, and related draft fields. Host/IP cannot change while saving in some flows — use new server flow for different IP.
Q053|“Host/IP cannot change while saving. Use a new server flow for a different IP.” Protects inventory integrity and chain mappings. Chain IP mismatch dialog appears if IP registered under a different chain number.
Q054|Check Panel Login Info URL/path, proxy, firewall, and re-run panel tools. Node/panel messages: central panel credentials missing → Complete Full Deploy; login fail often stale path/token → re-run Full Deploy or Panel Log
Q055|Ops can install WireGuard only or install/update Sanaei panel only on central. Useful when Full Deploy partially succeeded. Settings also “Check Update 3X-UI” to cache latest panel package for next install.
Q056|Read-only health checks for SSH, WireGuard, panel, DNS, CDN, and exits. Run Checks, review OK/Warning/Error chips, export logs for support.
Q057|Measure connectivity and recommend WireGuard, GRE fallback, or Reverse Tunnel (Stealth-WSS). AI template prefers recipes for paths/latency; apply via Mesh or Add Server link type.
Q058|If WireGuard tunnel fails, app offers GRE instead. Dialog title “WireGuard tunnel failed”; body asks whether to use GRE tunnel instead. Mesh failover order: WireGuard → GRE → Reverse Tunnel (Stealth-WSS) → backups.
Q059|Protected reverse tunnel option in Black Fox link/mesh stack. Product branding uses Reverse Tunnel (Stealth-WSS). Used when direct WG/GRE paths are poor; mesh monitors it via Link Monitor Agent.
Q060|Always-on agents on Central, Tunnel, Exit, Node hosts for path health and failover. Mesh wizard installs Link Monitor Agents; tools download from GitHub on each VPS at install time. Base always monitors WG, GRE, Stealth-
Q061|No — agents keep running on servers when the app is closed. Product copy: keeps running when this app is closed. App is for install/status/repair.
Q062|Re-install/deploy mesh and Refresh Status. Status labels: Agent installed / missing, Running / Stopped / Status unknown — Refresh Status. Deploy result: “Mesh agents installed: X ok, Y failed”.
Q063|Telegram bot wizard that downloads Mirza onto a chosen host and prefills panel credentials. Chooser: Install Mirza, save another bot, or move existing bot. Update flow: Back Up & Update Mirza. Needs bot token and admin T
Q064|Yes — bot chooser includes move existing bot. Move wizard migrates bot; backup recommended before update/move.
Q065|Multiple UI languages including English, Persian, Russian, Spanish, Arabic, German, French, Hindi, Turkish, and others shipped in i18n. Language picker at start; Settings → Language applies to every tab. Persian uses RTL
Q066|Setup installs under LocalAppData Programs; portable runs with a portable marker and local data beside the exe. Customer deliverable prefers `Black Fox Vpn-Installer-Setup.exe`. Portable uses `portable.txt` marker (`path
Q067|Clears local saved credentials/logs/deploy state; does not change remote servers; keeps language, mode, license. Removes SSH credentials, location files, panel logins, deploy status, session logs, subscriptions, CDN entr
Q068|Removes WireGuard/microsocks/firewall from exits; clears matching central tunnels; keeps SSH credentials. Wizard can delete all or one; separate node removal unregisters from central panel when possible.
Q069|Removes Pro chain hop installs; deleting one hop resets higher sequence hops. Exit servers still using those hops must be deleted first. Local TunnelServers settings cleared.
Q070|Removes WireGuard and Sanaei/x-ui from every configured server and clears local deployment data; keeps SSH credentials. Strongest in-app reset of deploys. Not the same as Factory Reset Server OS wipe.
Q071|Destructive remote reset toward initial VPS state; credentials for this op are not stored. Removes data, services, VPN packages, tunnels, firewall rules, users, custom settings. Unsupported OS fails. Confirm dialog is se
Q072|No — language, app mode, and license activation are kept. Explicit in delete history body. Factory/OS reinstall of Windows may still change Device ID behavior — use Reactivation.
Q073|Settings → Update BlackFox & Wallet Address; force update may block old versions. Force dialog: “A new BlackFox version is available” / “Your installed version is no longer supported. Download and install the latest rele
Q074|Downloads latest Sanaei panel package into local cache for future installs. Free download for future server installs; does not by itself upgrade a live panel unless you run install/update ops.
Q075|Windows is the full installer ops surface; Android is companion naming/engine packaging with shared AI persona. Android may require `BlackFox-VPN-Android-release.apk` (~large) when Go engine unavailable. AI prompts adjus
Q076|Telegram @HiBlackFoxVpn; site foxnext.net; email support@foxnext.net; channel @BlackFoxVPN. Contact tab lists official Telegram accounts. Support link key `contact.support_link` = @HiBlackFoxVpn. Website WWW.foxnext.net.
Q077|Device ID, app version, mode, operation name, terminal excerpt, TX/code type (not full secrets if avoidable). Diagnostic export helps. For license: Device ID + last TX + whether claim/reactivation. For SSH: host redacted
Q078|Local MCP bridge so external AI apps can use BlackFox tools while the app stays open. Activate MCP, copy mcp.json, paste into AI app MCP settings. Status: Ready / Connected / Not Activated. “BlackFox must stay open while
Q079|Unlock AI Pro on Registration (`view.ai_pro_locked`). Chat and MCP tasks check `IsAIProUnlocked()`. Basic/Pro alone is not enough for AI Assistant Pro.
Q080|Message asks to message support to renew; or use BFXQ recharge. `ai.quota.exhausted`: credit ended. Also expired / not_found / check_failed / disabled keys. Hub reason codes exhausted/expired.