Q161|No — modes share one local config store. Basic / Pro / AI Pro read the same central, exits, panel credentials, and op state. Switching View only changes UI shape and gates.
Q162|Full Deploy installs WG+panel on central; Configure Panel repairs routing for existing exit/node. Configure Panel is not “install panel again on central.” Use it after exits/nodes exist when inbound/outbound/SOCKS need r
Q163|Up to 6 nodes (`MaxNodes`). Same slot philosophy as exits: Pro Add Node Servers, capped at six.
Q164|Sanaei GitHub, BlackFox Hub, or Local PC package. Chooser appears in Full Deploy and node flows. Hub packages come from dual hubs; local is offline/air-gapped style.
Q165|Clears local history; keeps language, mode, and license. Distinct from DELETE — Reset All Servers (removes WG/panel from servers but keeps SSH) and factory-style wipes.
Q166|Removes WireGuard/panel from configured servers; keeps stored SSH credentials. Use when you want a clean software reset without retyping SSH. You must Full Deploy again afterward.
Q167|Yes — Delete Tunnel Servers resets Pro chain hops. Does not necessarily wipe exits/central; it targets tunnel-server slots in the Pro chain model.
Q168|None / Auto / Iran / Free-style profiles in Proxy Settings. Network policy tries Direct first, then Program Proxy on failure for SSH, hub HTTP, and panel API.
Q169|Panel Login Info (Help/repair area). Shows saved panel URL, user, and password from the shared store after successful panel install/sync.
Q170|Opens client/WireGuard test dialog to validate connectivity after configure. Use after Configure Panel / client creation — not a substitute for Full Deploy.
Q171|Pro DNS manager for Cloudflare / ArvanCloud zones and records. Can include optional bot webhook subdomain. Opens as overlay, not a top-level main tab.
Q172|Arvan, Cloudflare, and Other CDN in the Pro CDN area. A `CDNProviderKeyCDN` may exist in config code but is not listed in `AllCDNProviders()` for UI — treat as NEED_MORE_REVIEW if a user sees a fourth brand.
Q173|Pro inventory graph + deploy Link Monitor agents and link status. Overlay page separate from Operations grid. Used after topology exists to monitor/deploy mesh links.
Q174|Pro tool to install/move Mirza sales bot (or other bot path) on a server. Distinct from Black Fox support bot / Agent Bot. Mirza is the panel sales bot family (mahdiMGF2/botmirzapanel inspiration).
Q175|Relocate the central role to another VPS (Pro Tools). High-impact op — inventory and panel credentials must stay consistent. Exact wizard field list: NEED_MORE_REVIEW until live GUI capture.
Q176|Activates MCP binary / mcp.json so external AI tools can call Black Fox actions. AI Assistant Pro task path (`cmd/blackfox-mcp`). Not required for normal in-app chat ops.
Q177|Check System is the Diagnostics tab; Diagnose & Repair is an AI Pro chat task path. Diagnostics tab runs local/system-oriented checks. AI Diagnose queues guided repair actions with confirmation.
Q178|AI task template to test topology links. Complements Configure Panel and Test Client; uses AI confirmation flow before execution.
Q179|AI-assisted outbound configuration task. Targets panel outbound wiring; confirm selections before apply. Prefer after exits/nodes exist.
Q180|No — factory reset credentials are for that op only (credentials_hint). Unlike normal server save, factory reset does not keep those credentials for later ops.
Q181|Settings → language apply (also first-run language picker). Website language on foxnext.net is independent of app language catalogs.
Q182|Tools area to manage offline/hub 3X-UI packages used by deploy. Related to Local PC / Hub package sources for Full Deploy and nodes. Exact UI labels: verify against current Settings Tools.
Q183|Below minimum supported version/build, or force flag with newer remote. Hub `version.json` drives Windows/Android/Config Builder feeds. Local AppBuild may differ from published hub — do not invent numbers.
Q184|Separate admin tool (`cmd/pas-generator`) for offline/admin PAS workflows. Not the end-user Installer main path. End users normally activate via foxnext.net + Registration.
Q185|Separate Android APK listed in hub `version.json` → `config_builder`. Related product; source is not in this Windows repo tree. Do not confuse with main VPN Android app.
Q186|Iran, China, Russia (`IsFreeCentralCountry`) without Pro. Other central countries generally need Pro path. Confirm country picker labels in current UI.
Q187|No — AI Assistant Pro is a separate unlock + quota. Pro unlocks Pro dashboard ops. AI chat needs `ai_pro` / BFXA-style unlock and hub AI credit.
Q188|BFXB Basic, BFXP Pro, BFXA AI Pro, BFXQ AI quota, BFXT test; claim codes contain `-CLM-`. Enter on Registration Activate. Wrong prefix → wrong tier unlock or error.
Q189|Shows stored activation code after successful unlock. Useful for support tickets; still protect the code like a secret.
Q190|Machine fingerprint used to bind license. Required for Activation website and Reactivation. Changing hardware may break binding.
Q191|Often **18 months** in license code unless months encoded/selected. Live catalog months (12–30) and multi-device discounts come from `license-access.json`. Prefer on-screen duration.
Q192|Primary `blackfoxupdate.ir`, secondary `foxnext.net`. Dual-hub for version, runtime-config, license-access, AI proxy, packages. Failover when primary blocked.
Q193|Includes `@HiBlackFoxVpn`, `@BlackFoxVpnn`, `@BlackFoxVpn_bot`, `@Black_Fox_Group` (runtime contact). Prefer live Contact tab / runtime-config over memorized lists — values can change on hub.
Q194|Hub may show `github.com/balckfoxgroup/blackfox-vpn-installer` — use the published string. Do not “correct” the org spelling in customer replies if that is the live Contact link.
Q195|Use Windows Installer for Full Deploy / mesh ops; Android is companion. Android has chat/ops surfaces but Windows remains the primary SSH deploy workstation.
Q196|Risky — prefer one portable folder or Setup+LocalAppData. Portable mode stores data beside the exe. Two copies can fight the same servers conceptually if both deploy.
Q197|Version numbers, prices, hosting brands, Linux stderr not in i18n. Product AI persona: identity is BlackFox AI; mark gaps NEED_MORE_REVIEW; prefer catalog + this Documentation set.
Q198|`/Screenshots` with README index; live dialog capture still incomplete. Mode picker, Basic/Pro dashboards, Android onboarding, marketing heroes, Baner/BOT assets are present. Missing: Registration, wizards, Mesh, AI chat
Q199|Proxy → Connect SSH → Full Deploy source → Panel Login Info → Configure Panel → diagnostics. Don’t jump to Delete/Reset first. Confirm free Basic path vs license gate. Escalate with logs.
Q200|`Documentation/AI_Knowledge_Base.md` plus companions in the same folder. Use Product_Overview, Feature_List, guides, FAQ, Troubleshooting, Change_Log, and Screenshots/README together. Prefer accuracy overrides in FAQ hea