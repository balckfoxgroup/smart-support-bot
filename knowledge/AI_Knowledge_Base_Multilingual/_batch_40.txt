Q081|Up to 3 images and 5 text files per message. Exact i18n limits. Excess returns too_many_images / too_many_text_files on API.
Q082|No — design is analyze, ask confirmation, then run. Tags: User Confirmation, Safe by Design. Chat asks Yes/No before execute. Setting an action only queues; app reports wait/done/failed.
Q083|Wait until terminal/process finishes; do not close busy window casually. `ai.chat.op.wait` / done / failed. Close-busy dialog warns incomplete work if closing early.
Q084|Yes in AI Pro — paste outbound JSON; max 10 AI outbounds on panel. Deleting outbounds in panel UI does not reset the AI outbound counter. Invalid JSON rejected.
Q085|443 VLESS, 80 Trojan, 8080 WireGuard. From ops.hint.configure_panel. Actual panel may vary if customized — verify in Panel Login / client test.
Q086|Complete Full Deploy first (`extproxy.panel_missing` / `node.panel_missing`). Many ops depend on saved panel URL/user/pass/token after deploy.
Q087|Common after VPS reinstall; only accept if you trust the server. Dialog explains mismatch vs stored fingerprint and possible MITM. Accept clears stored key and retries.
Q088|Both invalid/expired; nothing invalid saved — re-enter correct details. Title “SSH authentication failed”; status “Invalid SSH key and password”.
Q089|App prompts Change Linux Password flow. Messages for current/new/confirm; success or failed/mismatch/required.
Q090|Network filter, wrong IP/port, provider firewall, or need proxy. Log key `log.ssh_timeout`. Not always a wrong password.
Q091|Mesh map after panel ready / links deployed with panel client. `view.topology_not_ready` explains prerequisites.
Q092|Add outbound routing entries (manual/AI custom v2ray outbounds on panel). Related to panel outbound wiring for exits and custom JSON outbounds in AI Pro.
Q093|CDN is in the Pro feature set. Pro desc lists CDN; Basic focuses on central+exits without Pro CDN positioning.
Q094|Cloudflare and ArvanCloud. Cloudflare: email + API token (Zone:DNS:Edit). Arvan: machine user + API key. Optional bot webhook subdomain separate from panel.
Q095|Optional separate subdomain for Telegram bot — skip if not needed. DNS bot webhook hint: not the panel; skip if webhook DNS unused.
Q096|Broader check/fix across disk/traffic, ping/filter, mesh links, domain/CDN, broken params. Available as ops/AI task; asks only when new IP or secret required.
Q097|Long operation in progress; wait for completion. Product invariant: show busy during long ops; remove when done; prefer duration feedback. Closing warns if busy.
Q098|Yes — Terminal Stop with confirm; marks operation stopped by user. Stop confirm title/body; irreversible interruption of current process.
Q099|Under `%LocalAppData%\Programs\Black Fox Vpn` (per Delete History text). Portable mode stores beside executable when portable marker present.
Q100|Dedicated Copy buttons with “copied to clipboard successfully” toast pattern. copy.device_id, copy.wallet, copy.tx_hash, copy.activation_code keys.
Q101|Host APIs can record offline codes/TX with claim_pending flags. Used when verifying payments and issuing claim codes; TX already used blocked. Exact UI stringing may vary — NEED_MORE_REVIEW for every offline dialog varia
Q102|Basic / Pro / AI Pro / AI quota. Machine and claim generators use these prefixes. Wrong prefix for target feature fails unlock checks (e.g. AI Pro feature requires AI kind).
Q103|Yes — AI Pro description: all Pro operations via chat + simultaneous manual Pro access. Auth: AI Pro unlock satisfies many Pro feature checks; AI Pro full is separate from generic Pro for AI feature gate.
Q104|No — AI Assistant Pro must never be granted by generic Pro unlock. Explicit in `IsFeatureUnlocked` for FeatureAIProFull.
Q105|Website issues claim codes after payment; app can also verify TX / paste codes. After code claimed, TX lookup won’t reveal codes again. App Registration is the bind point for Device ID.
Q106|Internal/admin tool to issue claim codes and inspect TX / AI Device ID status. Not the customer Installer. Customers use foxnext.net + Installer Registration.
Q107|Central Full Deploy / panel ready; license for add_exit; SSH to exit VPS. Without central panel, outbound wiring fails. Basic country rules apply to central, not necessarily exit country.
Q108|Chain integrity — exits still using hops must be removed first. Delete tunnel wizard warning states this explicitly.
Q109|Sequence index for multi-hop tunnel servers. Saving with IP already under another chain number is rejected. Deleting a hop resets higher sequences.
Q110|Exit deploy uses microsocks as part of SOCKS outbound path (removed on exit delete). Delete exit body mentions WireGuard, microsocks, and firewall rules removal.
Q111|Import `.conf` in WireGuard app on Windows/Android/iOS. Panel WG hint in i18n. Client email/remark appear in panel Clients tab after ensure inbound.
Q112|Product history uses panel ports such as 2053 in deployments; always trust Panel Login Info for your install. Exact listening port is whatever Full Deploy saved. Stale path/token is a common Add Node failure cause.
Q113|Proxy Settings help the app reach panel/internet; separate from VPN client proxy. Hint: proxy for 3X-UI panel and internet. Iran vs free profiles.
Q114|Trust Wallet / MetaMask / Binance history copy TxID. Documented in `reg.tutorial_body` (wallet activity → copy hash).
Q115|Win+R → msinfo32, or in-app copy system info control where provided. Tutorial mentions msinfo32 and copy system info button for support fingerprints.
Q116|Activation amount lookup fails and tells you to contact support (@HiBlackFoxVpn). `requiredFeatureAmount` error text directs to support when live/effective pricing missing.
Q117|Update host used alongside foxnext.net in deploy notes. Host deploy docs: app checks blackfoxupdate.ir then foxnext.net. Customers should still prefer official foxnext.net downloads.
Q118|Access gate title when activation/Pro required. Paired with need_activate / need_pro messages.
Q119|Yes — template “run Full Deploy on my central server” with SSH details. AI queues action; confirm; wait for op done/failed. Same underlying deploy as Operations button.
Q120|On open, may ask Continue or New. Saved chat on device; reply Continue or New.