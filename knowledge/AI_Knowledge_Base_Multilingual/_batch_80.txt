Q121|Migrate central server to a new VPS (AI/ops template). Needs source and destination SSH details; panel migration concepts exist in codebase. High-risk — backup first.
Q122|Test Client can show both; subscription is for client apps that pull updates. Add Subscription helps publish; VLESS is single inbound link.
Q123|Need panel ready and links/client present. `view.topology_not_ready` conditions.
Q124|From GitHub on each VPS at install time. Mesh wizard hint. Restricted VPS may need proxy/GitHub access on the server side.
Q125|CDN provider option including non-listed providers in AI Pro CDN task. AI assistant page notes CDN providers including Other CDN; Diagnose & Repair is separate task after CDN.
Q126|Product validates certain ports in flows (e.g. historical Hysteria2 8080 notes); prefer in-app validation messages. Configure Panel documents 8080 for WireGuard inbound in hints. Exact Hysteria strings: NEED_MORE_REVIEW 
Q127|Possible if user can sudo for installs; many scripts assume privileged access. Factory reset and deploy scripts are root-oriented. Prefer root unless you know your image.
Q128|Either works; both failing triggers both_failed dialog; PEM optional on forms. Key auth common on cloud images; password on panel resets. App supports both.
Q129|Credentials/draft stored locally without finishing deploy. `server.draft_saved`. Useful before continuing multi-page wizards.
Q130|Prefer Keep Open; Close Anyway leaves work incomplete. `dialog.close_busy_title/body`.
Q131|Settings → Language → Apply. Applies to every tab. Initial language picker also appears on first run.
Q132|Support @HiBlackFoxVpn, channel @BlackFoxVPN, website foxnext.net, email support@foxnext.net. All contact accounts are Telegram accounts per note; tap opens browser/Telegram.
Q133|Telegram support handle is primary in product contact keys; email exists as support@foxnext.net. Prefer Telegram for speed with Device ID.
Q134|Settings action to refresh app update metadata and wallet display used for payments. Keeps payment address and version info current from hub.
Q135|Install the latest release; old builds are unsupported. `remote.force_title/body`. Hub ForceRequired when MustForceUpdate.
Q136|Retry; check internet/proxy; messages are connection timeout / could not reach AI service. `ai.chat.error.timeout` and `ai.chat.error.network`.
Q137|`ai.disabled` — hub disabled AI. API reason `ai_disabled`. Not a local license issue necessarily.
Q138|Activate AI Assistant Pro first (`ai.quota.not_found`). Distinct from exhausted. Mode required / quota_not_found reason codes.
Q139|No — Reset All keeps SSH credentials; Delete History removes local credentials. Important misconception: Reset All ≠ wipe local secrets; Delete History does wipe local inventory including SSH creds but keeps license.
Q140|No — it aims to strip the VPS toward initial state. Irreversible. Different from Delete All (app-driven package removal with kept SSH creds).
Q141|Refresh agent status; path recovery is automatic within a few seconds typically. Agents hint: recovery automatic; failover order WG→GRE→Stealth-WSS→backups.
Q142|Central, Tunnel, Exit, and Node hosts. Mesh wizard hint lists those roles.
Q143|Not the design — it’s an operational deploy library; Panel Login Info is one feature after deploy. Without Full Deploy, most automation won’t have targets.
Q144|One shared local store for servers/panel — not parallel AI-only config. Product invariant: shared store; panel login shared across modes.
Q145|Check System is read-only diagnostics; Diagnose & Repair attempts fixes. Use read-only first for support evidence; repair when you want automated remediation.
Q146|Re-run Configure Panel, verify outbound/link, SSH to exit, and diagnostics. Common causes: exit deploy incomplete, WG/GRE not up, panel outbound tag mismatch, firewall.
Q147|Stale panel path or token — re-run Full Deploy or Panel Login Info. Exact message in `node.err_central_login`.
Q148|Higher if you move/delete the portable folder; installed mode uses LocalAppData. Portable marker enables portable paths. Backup the folder if you rely on portable.
Q149|foxnext.net has localized marketing/FAQ pages; app has its own i18n catalogs. Website locales ≠ automatically changing app language — set language in app Settings.
Q150|Describes managing central + multiple exit/tunnel/node locations from one installer. Core value: multi-location topology automation rather than single VPS VPN only.
Q151|No — credentials used only for that operation and never stored (factory_reset.credentials_hint). Contrast with normal server save which does store SSH for ops.
Q152|Auth worked but remote probe didn’t match expected output (`log.ssh_probe_unexpected`). May indicate unusual shell/MOTD/busybox or blocked commands. NEED_MORE_REVIEW for exact probe command text.
Q153|Yes — up to 3 images; AI is multimodal (text + image). Placeholder mentions photo with details (server info, Telegram bot, etc.).
Q154|Decline and resend with clarification; chat has reject path. `ai.chat.confirm.rejected` asks to clarify purpose and resend SSH details or pick a task.
Q155|App is applying queued actions from the AI response. Status keys include preparing, connecting, generating, parsing, applying_actions, finalizing.
Q156|Those are common defaults/anchors; live catalog by months may differ. Always pay the amount shown on foxnext.net / in-app for your selected months and tier.
Q157|Server record exists but doesn’t match requested tier (API `tier_mismatch`). May need to activate the correct tier or contact support to align records.
Q158|Generally no — after claim, TX no longer reveals codes. PAS help explicit. Use Reactivation on bound device or support with Device ID.
Q159|Install Setup → language → Basic → save central → Connect SSH → Full Deploy → Panel Info → optional exit. Activate license when free keys no longer suffice. Use Proxy if network restricted. Contact @HiBlackFoxVpn if bloc
Q160|`internal/i18n/en.go` / `locales/en.json` and host API `reason_code`s. This FAQ summarizes behavior; if a screenshot string differs, prefer i18n keys over paraphrases. Mark unknown Linux stderr as NEED_MORE_REVIEW.